/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i.imgur.com',
      'res.cloudinary.com',
      'randomuser.me',
      'images.unsplash.com',
      'via.placeholder.com'
    ],
  },
};

module.exports = nextConfig;
